import styled from 'styled-components';
import { description, bold, regular } from '../../../../../assets/css/theme';

export const MemberName = styled.div`
    ${description};
    font-weight: ${bold};
`;

export const MemberEmail = styled.div`
    ${description};
    font-weight: ${regular};
`;
